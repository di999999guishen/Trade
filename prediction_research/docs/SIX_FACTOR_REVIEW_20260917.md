# 六因子比例复核与待办执行记录（2026-09-17）

结论：正式配置暂时保持 30/25/10/10/10/15；值得对资金类集中度做影子试验，但当前没有足够收益样本支持直接更换生产权重。六项指标不是六份独立信号。以下为本次冻结截面的诊断，不是收益回测，也不是仓位比例建议。

## 本次实际完成

- 核实上午 daily 的上证50ETF（510050.SS）、煤炭ETF（515220.SS）均已完成，评级均为 Hold；没有重复调用大模型。
- 修复 `daily_predict.py`：每轮唯一 run_id、完整结构化结果及 Markdown 保存，移除原来的 5000 字符截断，避免同分钟结果覆盖；日志目录先创建再打开。旧文件无法据此恢复已丢弃内容。
- 新增 `prediction_research.weight_audit`：核验原始快照 SHA256，按冻结决策时点重放当日截面，校验合格数量、入选顺序和分数，计算缺失率、截断数量、贡献离散程度、成对相关性、逐因子移除和影子比例敏感性。不写生产数据库或配置。
- 复核今日 cycle 归档：400 个文件通过完整性校验，issues/missing 均为空；归档成功不等于研究流程成功。
- 研究模块及保存回归共 179 项通过；随后新增的权重审计测试及保存测试 2 项通过（其中保存测试重复，因此共 180 项不同测试）。语法编译和 `git diff --check` 通过。

## 截面实测

输入为 `screen_etf_flow_20260917_095846_713602.json` 引用的上午快照；抓取时间 09:58，合格 628 只，同组筛选后 128 只。全部六项在本截面都有值。原始数据 SHA256 为 `f8b6d229631681cff5d380c13e59855521843ff7e08b9d14e681b847cc644a8c`。

机器可读明细：[diagnostics.json](../../reports/weight_audit_20260917/diagnostics.json)。

| 因子 | 正式比例 | 因子贡献的截面标准差 | 移除此项后前20重合数 | 解释 |
|---|---:|---:|---:|---|
| 主力净流入占比 | 30% | 0.10116 | 8 | 对当前排序影响最大 |
| 成交额流动性 | 25% | 0.03521 | 15 | 保留可交易性偏好；名义权重不等于排序影响 |
| 单日动量 | 10% | 0.00855 | 20 | 本截面影响较小，但不能推断长期无效 |
| 超大单／大单背离 | 10% | 0.05382 | 14 | 影响超过同权重动量，628只中96只触及正负截断值 |
| 资金流／市值 | 10% | 0.02560 | 16 | 与主力流共享资金信息 |
| 同类相对强度 | 15% | 0.02640 | 18 | 当前使用单日涨幅，并非中期趋势 |

标准差只描述本截面贡献离散程度，不是收益归因；各项存在协方差，不能直接相加得到独立贡献比例。逐项移除后剩余可用权重会按生产规则重新归一化。

主力流与资金流／市值 Pearson 相关系数 **0.6966**；单日动量与同类相对强度为 **0.9748**。后两项在同一比较组内主要是同一涨幅的缩放和平移，因此不宜当成独立确认。当前“同类”实际为同资产类别、同市场范围，不等同于同底层指数或同赛道。

流动性为非负对数分数，其余因子通常有正负值；流动性较强可以给总分提供正贡献，因此粗筛分不能解释为上涨概率。资金流三项合计50%，但对实际排序的影响还取决于分布、截断及相关性。

## 比例是否调整

| 因子 | 当前正式 | 待验证影子方案 |
|---|---:|---:|
| 主力净流入占比 | 30% | 25% |
| 流动性 | 25% | 25% |
| 单日动量 | 10% | 10% |
| 大小单背离 | 10% | 5% |
| 资金流／市值 | 10% | 10% |
| 同类相对强度 | 15% | 25% |

影子方案是“降低资金三项合计至40%、降低背离尖峰影响、增加价格类合计至35%”的结构性假设，**不是优化出来的最优比例**。本截面前20重合15只，变化5只。相对强度仍高度相关于单日动量，因此这只是资金类与价格类之间的重新分配，并未增加独立信息。

正式替换之前，应按同一决策时点和可用候选池累计至少60个有效观察日，等待5/20日标签成熟，比较共同样本上的扣成本收益、覆盖率、换手及不同市场阶段稳定性。现有 `flow_strategy_20260917_104813_640034.json` 只有3个观察日、2个及时筛选日，状态为 `waiting_for_history_or_prices`。60天也只是必要门槛，还需要历史价格和到期标签完整。

筛选比例的调优只能使用训练／验证段，最终留出段用于评价，不能按最终测试表现反复调权重。参见 [scikit-learn 数据泄漏说明](https://scikit-learn.org/stable/common_pitfalls.html#data-leakage)。不以 Buy 数量或一次截面排名变化作为有效性标准。

## 尚未完成及真实阻塞

1. 上午 cycle 为 `partial_failure`，本轮量化预测0条：159205、159272日线截至9月16日，与本轮盘中截面日期不匹配；589510、515940历史刷新失败；最终不满足至少两只可预测候选。调整权重不能修复日期口径和新基金历史不足。
2. 后续正式日线预测需要完整盘后数据。当前规则为“同日首次筛选冻结”，直接下午重跑不会自动替换上午冻结候选；需要明确区分盘中观察批次与盘后决策批次，保留原批次，不得偷偷重写历史或绕开时间门。该批次设计尚未在本次实现。
3. 研究配置仍为跳过 TradingAgents，独立 daily 的两只 Hold 不等于 research 前5名的深度分析完成。自动调度接入及异地备份目的地尚未确认，没有更改系统任务。
4. 历史资金快照、完整候选／对照价格、到期标签、交易成本及持仓级回测仍需后续积累。重复运行不能创造历史观察日。
5. 当前名称分类仍需合同级核对：本次候选里出现财富宝、日利名称，不能把名称推断视为已验证资产分类。权重验证前应完成产品类型及同类比较组审计。

复现本次权重诊断（输出须使用未存在的新文件名）：

```powershell
& .venv/Scripts/python.exe -m prediction_research.weight_audit prediction_research/runs/screen_etf_flow_20260917_095846_713602.json reports/weight_audit_20260917/diagnostics_replay.json
```
